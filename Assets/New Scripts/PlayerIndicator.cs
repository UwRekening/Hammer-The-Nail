using UnityEngine;

namespace New_Scripts
{
    /// <summary>
    /// Marker component to indicate this GameObject is the player.
    /// Used to avoid string-based tag checks.
    /// </summary>
    public class PlayerIndicator : MonoBehaviour
    {
        // No logic needed – used only for identification
    }
}